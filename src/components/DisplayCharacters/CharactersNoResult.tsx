import { Flex } from "@chakra-ui/react";
import React from "react";

const CharactersNoResult = () => {
  // TODO: Better component
  return <Flex width="100%">CharactersNoResult</Flex>;
};

export default React.memo(CharactersNoResult);
