import { Flex } from "@chakra-ui/react";
import React from "react";

const CharactersError = () => {
  // TODO: Better component
  return <Flex width="100%">CharactersError</Flex>;
};

export default React.memo(CharactersError);
